<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>SpeechCare - Therapists</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #111;
      color: white;
      font-family: 'Arial', sans-serif;
    }
    
    .therapists-container {
      max-width: 1000px;
      margin: 40px auto;
      background-color: #222;
      border-radius: 15px;
      padding: 30px;
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.3);
    }
    
    .logo {
      font-size: 2.5rem;
      font-weight: bold;
      color: white;
      margin-bottom: 20px;
    }
    
    .therapist-card {
      background-color: #333;
      border-radius: 10px;
      padding: 20px;
      margin-bottom: 20px;
    }
    
    .btn-primary {
      background-color: #5046e5;
      border: none;
    }
    
    .btn-primary:hover {
      background-color: #4033c0;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="therapists-container">
      <div class="logo">SpeechCare</div>
      
      <h2 class="mb-4">Our Speech Therapists</h2>
      
      <% therapists.forEach(therapist => { %>
        <div class="therapist-card">
          <h3><%= therapist.name %></h3>
          
          <div class="row">
            <div class="col-md-6">
              <h4>Specialties</h4>
              <p><%= therapist.specialties.join(', ') %></p>
              
              <h4>Languages</h4>
              <p><%= therapist.languages.join(', ') %></p>
            </div>
            
            <div class="col-md-6">
              <h4>Availability</h4>
              <p>Days: <%= therapist.availableDays.join(', ') %></p>
              
              <h4>Location</h4>
              <p><%= therapist.location %></p>
              
              <p>Current Caseload: <%= therapist.currentCaseload %> / <%= therapist.maxCaseload %></p>
            </div>
          </div>
        </div>
      <% }); %>
      
      <div class="mt-4">
        <a href="/patients/register" class="btn btn-primary">Register as Patient</a>
        <a href="/" class="btn btn-outline-light ms-3">Back to Home</a>
      </div>
    </div>
  </div>
  
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

